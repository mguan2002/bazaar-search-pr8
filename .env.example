# Local Docker Postgres (host port 5433 → container 5432). Re-point this to S1's
# managed Postgres at integration time — no code changes needed.
DATABASE_URL=postgresql+psycopg://bazaar:bazaar@localhost:5433/bazaar
